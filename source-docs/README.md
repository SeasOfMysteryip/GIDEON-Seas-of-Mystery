# Source Documents

This folder is reserved for local provenance files referenced by `source_file` fields.

The repo tracks this note only. Original `.docx` and `.pdf` exports should stay local unless they have been explicitly cleared for repository storage.
